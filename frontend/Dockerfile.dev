# Desenvolvimento com HMR (Vite). Código montado via volume; node_modules em volume nomeado.
FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

# Garante node_modules no volume ao subir; depois inicia o dev server
CMD ["sh", "-c", "npm install && npm run dev"]
